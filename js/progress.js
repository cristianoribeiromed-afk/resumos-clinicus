/* ==========================================================================
   ClinicusMed — progress.js
   Estado do Dashboard Demonstrativo e da Jornada Médica (10 semestres,
   progresso, cadeados, medalhas) — tudo via localStorage, sem backend.
   Construído na Etapa 3.

   Nota de transparência: esse módulo controla uma PRÉVIA/demonstração
   da futura plataforma (ver .cx-badge--preview em components.css) — os
   dados aqui não representam progresso real do aluno logado, já que
   ainda não existe sistema de conta/login na plataforma estática.
   ========================================================================== */

window.CxProgress = (function(){
  'use strict';

  function init(){
    // Implementação chega na Etapa 3, junto com o Dashboard Demonstrativo
    // e a seção "Jornada Médica".
  }

  return { init: init };
})();
