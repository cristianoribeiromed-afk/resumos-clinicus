/* ==========================================================================
   ClinicusMed — animations.js
   Scroll reveal (.cx-reveal -> .cx-in-view) e demais microanimações de
   entrada. Construído na Etapa 6, junto com o polimento final.
   Respeita prefers-reduced-motion (checado antes de observar qualquer
   elemento — se o usuário pediu menos movimento, os elementos .cx-reveal
   já aparecem visíveis via CSS, então esse módulo simplesmente não
   precisa fazer nada).
   ========================================================================== */

window.CxAnimations = (function(){
  'use strict';

  function init(){
    // Implementação chega na Etapa 6.
  }

  return { init: init };
})();
